---
services:
    dash:
        build:
            context: ./
            dockerfile: ./Dockerfile
        image: colinsedgwick/sequence-splicer-translator:1.0
        user: "1000:1000"
        ports: 
          - "8050:8050"
        command:
            "python3 app.py"